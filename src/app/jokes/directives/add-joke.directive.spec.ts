import { AddJokeDirective } from './add-joke.directive';

describe('AddJokeDirective', () => {
  it('should create an instance', () => {
    // const directive = new AddJokeDirective(null, null);
    // expect(directive).toBeTruthy();
  });
});
